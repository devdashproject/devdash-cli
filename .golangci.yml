version: "2"

linters:
  enable:
    - govet
    - staticcheck
    - unused
    - ineffassign
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
      - path: testutil\.go
        linters:
          - errcheck

formatters:
  enable:
    - gofmt
